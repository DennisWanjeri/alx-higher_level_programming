#!/usr/bin/python3


def divisible_by_2(my_list=[]):
    new_str = ""
    for i in range(len(my_list)):
        if my_list[i] % 2 == 0:
            new_str += "True"
        else:
            new_str += "False"
    return new_str
